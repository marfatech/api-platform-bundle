name: PHPUnit

on: [pull_request]

jobs:
  run:
    runs-on: 'ubuntu-latest'
    strategy:
      matrix:
        php-versions: ['8.0', '8.1']
        symfony-versions: ['~4.4', '~5.4', '~6.0']

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-versions }}
          tools: composer
        env:
          COMPOSER_TOKEN: ${{ secrets.COMPOSER_TOKEN }}

      - name: Configure symfony/flex
        run: composer config --no-plugins allow-plugins.symfony/flex true

      - name: Export Symfony version ${{ matrix.symfony-versions }}
        run: export SYMFONY_REQUIRE=${{ matrix.symfony-versions }}

      - name: Composer update on php ${{ matrix.php-versions }} and symfony ${{ matrix.symfony-versions }}
        run: composer update --prefer-dist --no-progress

      - name: Run tests
        run: |
          ./vendor/bin/phpunit

